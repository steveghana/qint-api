// import { init } from './api-gateway/main';

// import language from './util/language';
// init();

// console.log('process.env.PORT:', process.env.PORT);

// // smsUtil.init();
// // emailUtil.init();
// // pushUtil.init();
// // language.initialize();
// process
//   .on('unhandledRejection', (exception: Error) => {
//     logger.error(null, 'Unhandled rejection at Promise', [exception]);
//   })
//   .on('uncaughtException', (exception: Error) => {
//     logger.error(null, 'Unhandled exception thrown', [exception]);
//   });

// export default init;
